Steps

1. Create Aurora serverless databases with credintals

2. Create a secrets manager secret to hold the databases password

3. Created python script queries database secrets manager

4. Secrets manager gets the secret,decrpts it and returns the password to the application

5. Application parses the credentials and uses them to access the databases
